app:
  telegram-token: ${TOKEN}
  available-sites:
    - github.com
    - stackoverflow.com

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

server:
  port: 8090

logging:
  config: classpath:log4j2-plain.xml
